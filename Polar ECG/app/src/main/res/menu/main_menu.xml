<menu xmlns:android="http://schemas.android.com/apk/res/android"
      xmlns:app="http://schemas.android.com/apk/res-auto">
    <item
        android:id="@+id/pause"
        android:icon="@drawable/ic_pause_white_36dp"
        android:title="@string/pause_item"
        app:showAsAction="always"
        />
    <item
        android:id="@+id/stop"
        android:title="@string/stop_item"
        android:icon="@drawable/ic_stop_white_36dp"
        app:showAsAction="always"
        />
    <item
        android:id="@+id/save"
        android:title="@string/save_item"
        app:showAsAction="never"
        />
</menu>